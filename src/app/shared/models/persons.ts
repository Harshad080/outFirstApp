export interface Iperson {
    fname : string,
    lname : string,
    email : string,
    contact : number
};

export interface Iperson2 {
    empname : string,
    department : string,
    salary : number,
    empid : number,
}